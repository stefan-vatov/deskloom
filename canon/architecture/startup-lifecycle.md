---
status: normative
---
# Startup Lifecycle

Default-snapshot restore is opt-in: it runs only when startup restore is
enabled and a default snapshot is selected. The startup restore reconciles
additively, leaving unmatched windows alone, and must run once for the desktop
rather than once per monitor or bar instance.
